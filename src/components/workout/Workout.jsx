import React from 'react'
export default function Workout() {
  return (
    <div style={{ padding: '32px 28px' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: 32, marginBottom: 8 }}>ANTRENMAN</h1>
      <p style={{ color: 'var(--text-dim)', fontSize: 14 }}>Full Body / Split planlama, hareket önerileri, overtraining uyarısı — yakında</p>
    </div>
  )
}
